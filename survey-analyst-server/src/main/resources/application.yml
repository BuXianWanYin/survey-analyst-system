spring:
  profiles:
    active: dev
  application:
    name: survey-analyst-server

server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

